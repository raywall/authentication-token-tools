# .goreleaser.yml
version: 2

# Opcional: defina o nome do projeto se necessário
project_name: authentication-token-tools

builds:
- id: authentication-token-tools
  main: ./examples/lambda-authorizer.go
  binary: authentication-token-tools
  env:
  - CGO_ENABLED=0
  goos:
  - linux
  - windows
  - darwin
  goarch:
  - amd64
  - arm64
  - arm
  skip: true

release:
  # Opcional: você pode remover a seção 'github' se o repositório 
  # no qual você está executando a action for o 'raywall/authentication-token-tools'.
  # GoReleaser infere isso automaticamente.
  name_template: "Release {{ .Tag }}"
  github:
    owner: raywall
    name: authentication-token-tools
  make_latest: true

changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
    - 'Merge pull request'
    - 'Merge branch'
